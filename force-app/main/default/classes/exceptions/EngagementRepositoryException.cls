/**
 * Thrown by repository classes when a data access operation cannot be
 * completed (e.g. insufficient field/object access, invalid Id).
 */
public class EngagementRepositoryException extends Exception {
}
