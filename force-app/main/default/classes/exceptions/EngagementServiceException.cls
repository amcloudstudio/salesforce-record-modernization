/**
 * Thrown by service classes when a requested operation violates a business
 * rule (e.g. invalid status transition) or the current user lacks the
 * object/field access required to complete it.
 */
public class EngagementServiceException extends Exception {
}
